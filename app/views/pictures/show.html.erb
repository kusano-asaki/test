<h2><%= @picture.user.name %>さんの詳細画面</h2>
<div class="user_page">
  <p>image:<br><%= image_tag @picture.image.url %></p>
  <p>内容: <%= @picture.content %></p>
</div>
<p>コメント一覧</p>
<div id="comments_area">
  <%= render partial: 'comments/index', locals: { comments: @comments, blog: @blog } %>
</div>
<%= render partial: 'comments/form', locals: { comments: @comments, blog: @blog } %>
<%= link_to '編集', edit_picture_path(@picture) %>
<%= link_to '戻る', pictures_path %>
